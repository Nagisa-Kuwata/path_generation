# speckit_path_generation Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-24

## Active Technologies

- Rust stable（MSRV を `Cargo.toml` の `rust-version` で宣言） + `egui` + `eframe`（UI）、`rand` + `rand_chacha`（乱数）、`ordered-float`（A\* 優先キュー） (001-robot-maze-simulator)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test; cargo clippy

## Code Style

Rust stable（MSRV を `Cargo.toml` の `rust-version` で宣言）: Follow standard conventions

## Recent Changes

- 001-robot-maze-simulator: Added Rust stable（MSRV を `Cargo.toml` の `rust-version` で宣言） + `egui` + `eframe`（UI）、`rand` + `rand_chacha`（乱数）、`ordered-float`（A\* 優先キュー）

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
